extends layouts/main.pug
include mixins/videoBlock

//- <p>Hello</p>
//- p Hello

block content
    .videos
        each item in videos
            +videoBlock({
                id: item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            //- h1= item.title
            //- p= item.description